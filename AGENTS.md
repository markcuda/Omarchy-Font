# Agent Instructions

## GitHub Account Rule

This repository is locked to `markcuda/Omarchy-Font` using the `markcuda` GitHub account. Before pushes, PRs, or GitHub CLI operations, verify `.codex/repo-lock.json`, `git remote -v`, and `gh auth status`. Do not use another GitHub account for this repo unless the user explicitly changes the lock.
